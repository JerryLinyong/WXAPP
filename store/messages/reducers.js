import { LOGIN,LOGOUT } from './actionTypes'

let initMessages={

}

export default function account(state =initMessages, action) {
    switch (action.type) {
        default:
            return state
    }   
  }