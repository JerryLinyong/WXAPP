import { LOGIN,LOGOUT } from './actionTypes'

let initIdentity={

}

export default function account(state =initIdentity, action) {
    switch (action.type) {
        default:
            return state
    }   
  }