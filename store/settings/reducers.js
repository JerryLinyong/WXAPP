import { LOGIN,LOGOUT } from './actionTypes'

let initSettings={

}

export default function account(state =initSettings, action) {
    switch (action.type) {
        default:
            return state
    }   
}